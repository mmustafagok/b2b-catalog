# Shopify CLI web process configuration for CatalogFlow Node/Express/Vite app
name = "CatalogFlow Web App"
roles = ["frontend", "backend"]
webhooks_path = "/api/webhooks"
port = 8088

[commands]
dev = "npm run dev"

[server]
port = 8088
